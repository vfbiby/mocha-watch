import React from 'react';

describe('Visible TodoList connected component', () => {
  it("can be render with a list", function(){
    expect(1).to.equal(1)
/*    let spy = sinon.spy()*/

    //const visibleTodoList = mount(<VisibleTodoList />)

    /*expect(visibleTodoList.find('li').length).to.equal(3)*/
  })
});
